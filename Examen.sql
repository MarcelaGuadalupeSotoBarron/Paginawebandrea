create database examen;
use examen;
create table contenido(
id int auto_increment primary key,
modelo varchar(50),
marca varchar(50),
talla varchar(50),
codigo varchar(50),
precio varchar(50),
foto  varchar(100)
);
